# ──────────────────────────────────────────────────────────────────
# Dependências do Agente de Cotação PcD (versões fixas para deploy)
# ──────────────────────────────────────────────────────────────────
# Target: Python 3.11 (Railway via runtime.txt) — compatível com venv local 3.12.
# Use >= para permitir patches; pinar com == em deploy crítico.

# UI (legacy — Streamlit fica vivo durante a migração para FastAPI+Angular)
streamlit>=1.54.0,<2.0

# Backend novo (FastAPI substituirá Streamlit em produção; sobe em paralelo)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic-settings>=2.5.0,<3.0

# HTTP cliente assíncrono (Skiplagged via MCP/REST; clientes futuros migrarão de requests)
httpx>=0.27.0,<1.0

# Scraping headless (Skiplagged — fallback quando MCP/httpx não basta)
playwright>=1.48.0,<2.0

# Dados / utilitários
pandas>=2.3.0,<3.0
requests>=2.32.0,<3.0
python-dotenv>=1.1.0,<2.0
pydantic>=2.11.0,<3.0

# LLM (mantido apenas para summarizer final via Groq)
litellm>=1.75.0,<2.0

# ── Produto chat (independente do gerencial) ────────────────────
# Banco — Neon Postgres acessado via psycopg 3 (sync API, connection pool nativo)
psycopg[binary,pool]>=3.2.0,<4.0

# Orquestração de agentes
langgraph>=0.2.50,<1.0
langchain-anthropic>=0.3.0,<1.0
langchain-core>=0.3.30,<1.0

# Geração de PDF (relatório final para o vendedor)
# WeasyPrint depende de bibliotecas C nativas; fallback simples com reportlab disponível
# se WeasyPrint falhar no ambiente alvo (Railway/Windows).
weasyprint>=63.0,<70.0
reportlab>=4.2.0,<5.0
jinja2>=3.1.0,<4.0

# Rate limiting por endpoint/usuário
slowapi>=0.1.9,<1.0

# JWT validation contra JWKS (Neon Auth)
pyjwt[crypto]>=2.9.0,<3.0
