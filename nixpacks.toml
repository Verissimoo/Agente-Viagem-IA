[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
]

[start]
cmd = "streamlit run streamlit_app_multiagent.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
